# Terraform local artifacts — never commit these. (.terraform.lock.hcl IS committed.)
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
